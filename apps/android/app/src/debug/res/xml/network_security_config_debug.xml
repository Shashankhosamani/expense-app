<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (http://) is allowed only for the emulator's host-loopback alias
  and common private-LAN ranges, only in debug builds — for reaching a local
  `pnpm worker` (:8787) without HTTPS. Everything else still requires TLS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Testing on a physical device against a worker on your LAN?
             Network security config only matches exact hosts, not CIDR
             ranges — add your machine's LAN IP here too, e.g.:
             <domain includeSubdomains="false">192.168.1.23</domain> -->
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
